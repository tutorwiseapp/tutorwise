# Test Environment Configuration
# Copy this file to .env.test and fill in with actual test credentials

# ============================================
# IMPORTANT: Test-Only Credentials
# ============================================
# These should be dedicated test accounts in a Supabase TEST project
# DO NOT use production user credentials
# DO NOT commit .env.test to version control (already in .gitignore)

# Test User Credentials for E2E Tests
# --------------------------------------------
# Create these test users in your Supabase test project:
#
# 1. Test Tutor (provider role)
TEST_TUTOR_EMAIL=test-tutor@tutorwise.com
TEST_TUTOR_PASSWORD=TestPassword123!

# 2. Test Client (seeker role)
TEST_CLIENT_EMAIL=test-client@tutorwise.com
TEST_CLIENT_PASSWORD=TestPassword123!

# 3. Test Agent (agent role)
TEST_AGENT_EMAIL=test-agent@tutorwise.com
TEST_AGENT_PASSWORD=TestPassword123!

# Application Base URL
# --------------------------------------------
BASE_URL=http://localhost:3000

# Supabase Test Project (for integration tests)
# --------------------------------------------
# SUPABASE_TEST_URL=https://xxxxx.supabase.co
# SUPABASE_TEST_ANON_KEY=xxxxx
# SUPABASE_TEST_SERVICE_ROLE_KEY=xxxxx (for cleanup/setup scripts)

# Percy Visual Testing (Optional)
# --------------------------------------------
# PERCY_TOKEN=xxxxx

# Test Configuration
# --------------------------------------------
HEADLESS=false  # Set to true for CI/CD
SLOW_MO=0       # Slow down Playwright actions by X ms (for debugging)
TIMEOUT=30000   # Default test timeout in ms
