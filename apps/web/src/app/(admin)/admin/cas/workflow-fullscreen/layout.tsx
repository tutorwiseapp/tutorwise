/** Phase 6D: CAS fullscreen layout decommissioned — pass children through. */
import { ReactNode } from 'react';

export default function FullscreenLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
