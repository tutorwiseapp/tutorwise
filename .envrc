# ============================================================================
# Tutorwise Project Environment Configuration
# ============================================================================
# This file is automatically loaded by direnv when you cd into the project
# Install direnv: brew install direnv
# Enable: Add 'eval "$(direnv hook zsh)"' to ~/.zshrc

# Node Version Management (optional - uncomment if using nvm/fnm)
# use node 22

# Project-specific environment variables
export PROJECT_NAME="tutorwise"
export NODE_ENV="development"

# Automatically load .env.local for Supabase/Stripe credentials
# Note: Don't commit .env.local - it contains secrets
dotenv_if_exists .env.local

# Automatically load .env.test when running tests
# dotenv_if_exists .env.test

# Development shortcuts (optional)
# alias dev='npm run dev'
# alias test='npm run test'
# alias build='npm run build'

# Allow direnv to load this configuration
direnv allow
